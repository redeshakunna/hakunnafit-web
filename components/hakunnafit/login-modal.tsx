// Deprecado: el login se unificó en una sola pantalla (/panel/login, ver
// lib/unified-login-actions.ts) que sirve tanto a entrenadores como al
// super-admin. Este modal de 2 pestañas ya no se usa en ningún lado (el
// header ahora enlaza directo a /panel/login) — se deja este archivo vacío
// en vez de borrado porque el sandbox no permite eliminar archivos; bórralo
// con confianza la próxima vez que toques este directorio desde tu máquina.
export {};
